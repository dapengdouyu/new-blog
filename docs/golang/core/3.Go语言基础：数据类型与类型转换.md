---
title: 3、Go语言基础：数据类型与类型转换
date: 2026-01-09
description: 深入学习Go语言的基本数据类型、类型系统以及类型转换的方法
---

# 3、Go语言基础：数据类型与类型转换

本文档深入讲解Go语言的数据类型系统和类型转换机制。

## 🎯 本课学习目标

完成本课后，你将能够：
- ✅ 掌握Go语言的基本数据类型
- ✅ 理解类型系统的特点
- ✅ 学会数值类型之间的转换
- ✅ 掌握类型断言的使用
- ✅ 理解类型转换的最佳实践

---

## 前置知识

- **第1课：基础语法与变量常量** - 了解变量和常量的基本概念

---

## 一、基本数据类型

### 1.1 数值类型

#### 整数类型

| 类型 | 大小 | 范围 |
|------|------|------|
| `int8` | 8位 | -128 到 127 |
| `int16` | 16位 | -32768 到 32767 |
| `int32` | 32位 | -2147483648 到 2147483647 |
| `int64` | 64位 | -9223372036854775808 到 9223372036854775807 |
| `int` | 平台相关 | 32位或64位 |
| `uint8` | 8位 | 0 到 255 |
| `uint16` | 16位 | 0 到 65535 |
| `uint32` | 32位 | 0 到 4294967295 |
| `uint64` | 64位 | 0 到 18446744073709551615 |
| `uint` | 平台相关 | 32位或64位 |
| `uintptr` | 平台相关 | 足够存储指针的整数 |

```go
package main

import "fmt"

func main() {
    var i8 int8 = 127
    var i16 int16 = 32767
    var i32 int32 = 2147483647
    var i64 int64 = 9223372036854775807
    var i int = 100  // 平台相关
    
    var u8 uint8 = 255
    var u16 uint16 = 65535
    var u uint = 100
    
    fmt.Printf("int8: %d, int16: %d, int32: %d, int64: %d\n", i8, i16, i32, i64)
    fmt.Printf("uint8: %d, uint16: %d, uint: %d\n", u8, u16, u)
}
```

#### 浮点数类型

| 类型 | 大小 | 精度 |
|------|------|------|
| `float32` | 32位 | 约6位小数 |
| `float64` | 64位 | 约15位小数 |

```go
package main

import "fmt"

func main() {
    var f32 float32 = 3.14159
    var f64 float64 = 3.141592653589793
    
    fmt.Printf("float32: %.5f\n", f32)
    fmt.Printf("float64: %.15f\n", f64)
}
```

#### 复数类型

| 类型 | 说明 |
|------|------|
| `complex64` | 实部和虚部都是float32 |
| `complex128` | 实部和虚部都是float64 |

```go
package main

import "fmt"

func main() {
    var c64 complex64 = 1 + 2i
    var c128 complex128 = 3 + 4i
    
    fmt.Printf("complex64: %v\n", c64)
    fmt.Printf("complex128: %v\n", c128)
    fmt.Printf("实部: %.1f, 虚部: %.1f\n", real(c128), imag(c128))
}
```

### 1.2 布尔类型

```go
package main

import "fmt"

func main() {
    var b1 bool = true
    var b2 bool = false
    var b3 bool  // 零值是false
    
    fmt.Printf("b1: %t, b2: %t, b3: %t\n", b1, b2, b3)
}
```

### 1.3 字符串类型

```go
package main

import "fmt"

func main() {
    var s1 string = "Hello"
    var s2 string = `多行
字符串`
    var s3 string  // 零值是""
    
    fmt.Println(s1)
    fmt.Println(s2)
    fmt.Printf("s3: %q\n", s3)
}
```

### 1.4 字节和字符

```go
package main

import "fmt"

func main() {
    // byte是uint8的别名
    var b byte = 'A'
    var b2 uint8 = 65
    
    // rune是int32的别名，表示Unicode码点
    var r rune = '中'
    var r2 int32 = 20013
    
    fmt.Printf("byte: %c (%d)\n", b, b)
    fmt.Printf("rune: %c (%d)\n", r, r)
    fmt.Printf("b == b2: %t, r == r2: %t\n", b == b2, r == r2)
}
```

---

## 二、类型系统特点

### 2.1 静态类型

Go是静态类型语言，变量类型在编译期确定：

```go
package main

func main() {
    var x int = 10
    // x = "hello"  // 编译错误：cannot use "hello" (type string) as type int
}
```

### 2.2 类型推断

Go支持类型推断，编译器可以自动推断变量类型：

```go
package main

import "fmt"

func main() {
    var x = 10        // 推断为int
    var y = 3.14      // 推断为float64
    var z = "hello"   // 推断为string
    
    fmt.Printf("x: %T, y: %T, z: %T\n", x, y, z)
    // 输出：x: int, y: float64, z: string
}
```

### 2.3 零值

每种类型都有零值：

```go
package main

import "fmt"

func main() {
    var (
        i int       // 0
        f float64   // 0.0
        b bool      // false
        s string    // ""
        p *int      // nil
    )
    
    fmt.Printf("int: %d, float: %.1f, bool: %t, string: %q\n", i, f, b, s)
    fmt.Printf("pointer: %v\n", p)
}
```

---

## 三、类型转换

### 3.1 数值类型转换

Go语言要求**显式类型转换**，不支持隐式转换：

```go
package main

import "fmt"

func main() {
    var i int = 42
    var f float64 = float64(i)  // 显式转换
    var u uint = uint(i)
    
    fmt.Printf("int: %d, float64: %.1f, uint: %d\n", i, f, u)
    
    // 隐式转换会编译错误
    // var f2 float64 = i  // 编译错误：cannot use i (type int) as type float64
}
```

#### 整数类型转换

```go
package main

import "fmt"

func main() {
    var i8 int8 = 100
    var i16 int16 = int16(i8)
    var i32 int32 = int32(i16)
    var i64 int64 = int64(i32)
    
    fmt.Printf("int8: %d, int16: %d, int32: %d, int64: %d\n", i8, i16, i32, i64)
    
    // 注意：大类型转小类型可能丢失数据
    var big int64 = 300
    var small int8 = int8(big)  // 可能溢出
    fmt.Printf("big: %d, small: %d\n", big, small)  // 300, 44（溢出）
}
```

#### 浮点数转换

```go
package main

import "fmt"

func main() {
    var f32 float32 = 3.14159
    var f64 float64 = float64(f32)
    
    var i int = 42
    var f float64 = float64(i)
    var i2 int = int(f64)  // 截断小数部分
    
    fmt.Printf("f32: %.5f, f64: %.5f\n", f32, f64)
    fmt.Printf("int: %d, float: %.1f, int from float: %d\n", i, f, i2)
}
```

### 3.2 字符串与数值转换

#### 使用strconv包

```go
package main

import (
    "fmt"
    "strconv"
)

func main() {
    // 字符串转整数
    i, err := strconv.Atoi("123")
    if err != nil {
        fmt.Println("转换失败:", err)
    } else {
        fmt.Printf("字符串转int: %d\n", i)
    }
    
    // 整数转字符串
    s := strconv.Itoa(123)
    fmt.Printf("int转字符串: %s\n", s)
    
    // 字符串转其他类型
    i64, _ := strconv.ParseInt("123", 10, 64)
    f64, _ := strconv.ParseFloat("3.14", 64)
    b, _ := strconv.ParseBool("true")
    
    fmt.Printf("ParseInt: %d, ParseFloat: %.2f, ParseBool: %t\n", i64, f64, b)
    
    // 其他类型转字符串
    s1 := strconv.FormatInt(123, 10)
    s2 := strconv.FormatFloat(3.14, 'f', 2, 64)
    s3 := strconv.FormatBool(true)
    
    fmt.Printf("FormatInt: %s, FormatFloat: %s, FormatBool: %s\n", s1, s2, s3)
}
```

### 3.3 字符串与字节转换

```go
package main

import "fmt"

func main() {
    // 字符串转字节切片
    s := "Hello"
    bytes := []byte(s)
    fmt.Printf("字符串: %s, 字节: %v\n", s, bytes)
    
    // 字节切片转字符串
    s2 := string(bytes)
    fmt.Printf("字节转字符串: %s\n", s2)
    
    // 字符串转rune切片（Unicode字符）
    runes := []rune("你好")
    fmt.Printf("rune切片: %v\n", runes)
    
    // rune切片转字符串
    s3 := string(runes)
    fmt.Printf("rune转字符串: %s\n", s3)
}
```

### 3.4 类型别名和自定义类型

#### 类型别名

```go
package main

import "fmt"

// 类型别名：完全相同的类型
type MyInt = int

func main() {
    var x int = 10
    var y MyInt = 20
    
    // 类型别名可以互相赋值
    x = y  // 可以
    y = x  // 可以
    
    fmt.Printf("x: %d, y: %d\n", x, y)
    fmt.Printf("x的类型: %T, y的类型: %T\n", x, y)  // 都是int
}
```

#### 自定义类型

```go
package main

import "fmt"

// 自定义类型：新类型，不能直接赋值
type Celsius float64
type Fahrenheit float64

func main() {
    var c Celsius = 25.0
    var f Fahrenheit = 77.0
    
    // 不能直接赋值
    // c = f  // 编译错误：cannot use f (type Fahrenheit) as type Celsius
    
    // 需要显式转换
    c = Celsius(f)
    f = Fahrenheit(c)
    
    fmt.Printf("Celsius: %.1f, Fahrenheit: %.1f\n", c, f)
}

// 为自定义类型添加方法
func (c Celsius) ToFahrenheit() Fahrenheit {
    return Fahrenheit(c*9/5 + 32)
}

func (f Fahrenheit) ToCelsius() Celsius {
    return Celsius((f - 32) * 5 / 9)
}
```

---

## 四、类型断言

### 4.1 interface{}类型断言

`interface{}`是空接口，可以表示任何类型：

```go
package main

import "fmt"

func main() {
    var x interface{} = 42
    
    // 类型断言：value, ok := x.(Type)
    if value, ok := x.(int); ok {
        fmt.Printf("x是int类型，值为: %d\n", value)
    }
    
    // 类型断言：value := x.(Type)（如果失败会panic）
    var y interface{} = "hello"
    str := y.(string)
    fmt.Printf("y是string类型，值为: %s\n", str)
}
```

### 4.2 类型开关（Type Switch）

```go
package main

import "fmt"

func printType(x interface{}) {
    switch v := x.(type) {
    case int:
        fmt.Printf("int类型: %d\n", v)
    case string:
        fmt.Printf("string类型: %s\n", v)
    case float64:
        fmt.Printf("float64类型: %.2f\n", v)
    case bool:
        fmt.Printf("bool类型: %t\n", v)
    default:
        fmt.Printf("未知类型: %v\n", v)
    }
}

func main() {
    printType(42)
    printType("hello")
    printType(3.14)
    printType(true)
    printType([]int{1, 2, 3})
}
```

---

## 五、类型转换的最佳实践

### 5.1 安全的类型转换

```go
package main

import (
    "fmt"
    "strconv"
)

// 安全的字符串转整数
func safeAtoi(s string) (int, error) {
    return strconv.Atoi(s)
}

// 安全的类型断言
func safeAssert(x interface{}) (int, bool) {
    value, ok := x.(int)
    return value, ok
}

func main() {
    // 总是检查错误
    if i, err := safeAtoi("123"); err == nil {
        fmt.Printf("转换成功: %d\n", i)
    } else {
        fmt.Printf("转换失败: %v\n", err)
    }
    
    // 检查类型断言是否成功
    var x interface{} = 42
    if value, ok := safeAssert(x); ok {
        fmt.Printf("断言成功: %d\n", value)
    }
}
```

### 5.2 避免数据丢失

```go
package main

import "fmt"

func main() {
    // 大类型转小类型可能丢失数据
    var big int64 = 300
    var small int8 = int8(big)
    fmt.Printf("big: %d, small: %d (可能溢出)\n", big, small)
    
    // 浮点数转整数会截断
    var f float64 = 3.9
    var i int = int(f)
    fmt.Printf("float: %.1f, int: %d (截断)\n", f, i)
    
    // 检查是否溢出
    if big > 127 || big < -128 {
        fmt.Println("警告：int64值超出int8范围")
    }
}
```

### 5.3 使用类型转换工具函数

```go
package main

import (
    "fmt"
    "strconv"
)

// 类型转换工具函数
type Converter struct{}

func (c *Converter) ToInt(s string) (int, error) {
    return strconv.Atoi(s)
}

func (c *Converter) ToFloat(s string) (float64, error) {
    return strconv.ParseFloat(s, 64)
}

func (c *Converter) ToBool(s string) (bool, error) {
    return strconv.ParseBool(s)
}

func main() {
    conv := &Converter{}
    
    if i, err := conv.ToInt("123"); err == nil {
        fmt.Printf("转换成功: %d\n", i)
    }
}
```

---

## 六、综合示例

```go
package main

import (
    "fmt"
    "strconv"
)

// 自定义温度类型
type Temperature float64

func (t Temperature) Celsius() float64 {
    return float64(t)
}

func (t Temperature) Fahrenheit() float64 {
    return float64(t)*9/5 + 32
}

func main() {
    // 1. 基本类型转换
    var i int = 42
    var f float64 = float64(i)
    fmt.Printf("int: %d, float64: %.1f\n", i, f)
    
    // 2. 字符串转换
    s := strconv.Itoa(123)
    i2, _ := strconv.Atoi("456")
    fmt.Printf("字符串: %s, 整数: %d\n", s, i2)
    
    // 3. 类型断言
    var x interface{} = "hello"
    if str, ok := x.(string); ok {
        fmt.Printf("类型断言成功: %s\n", str)
    }
    
    // 4. 类型开关
    printType(42)
    printType("world")
    printType(3.14)
    
    // 5. 自定义类型
    temp := Temperature(25.0)
    fmt.Printf("摄氏度: %.1f, 华氏度: %.1f\n", 
        temp.Celsius(), temp.Fahrenheit())
}

func printType(x interface{}) {
    switch v := x.(type) {
    case int:
        fmt.Printf("int: %d\n", v)
    case string:
        fmt.Printf("string: %s\n", v)
    case float64:
        fmt.Printf("float64: %.2f\n", v)
    default:
        fmt.Printf("其他类型: %v\n", v)
    }
}
```

---

## 总结

### 数据类型要点

1. **数值类型**：
   - 整数：int8到int64，uint8到uint64，int，uint
   - 浮点数：float32，float64
   - 复数：complex64，complex128

2. **其他类型**：
   - bool：布尔类型
   - string：字符串类型
   - byte：uint8的别名
   - rune：int32的别名

3. **类型系统**：
   - 静态类型：编译期确定
   - 类型推断：自动推断类型
   - 零值：未初始化变量的默认值

### 类型转换要点

1. **显式转换**：Go要求显式类型转换
2. **数值转换**：注意溢出和精度丢失
3. **字符串转换**：使用strconv包
4. **类型断言**：用于interface{}类型
5. **类型开关**：使用type switch处理多种类型

### 最佳实践

1. **安全转换**：总是检查转换错误
2. **避免丢失**：注意大类型转小类型的数据丢失
3. **使用工具**：使用strconv包进行字符串转换
4. **类型安全**：使用自定义类型增强类型安全性

掌握数据类型和类型转换是Go语言编程的基础。在下一课中，我们将学习运算符和位运算。