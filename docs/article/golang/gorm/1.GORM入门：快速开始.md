---
title: 1、GORM入门：快速开始
date: 2026-01-09
description: GORM框架快速入门，了解基本概念、安装配置和第一个数据库操作
---

# 1、GORM入门：快速开始

本文档将带你快速上手GORM框架，创建你的第一个数据库操作应用。

## 🎯 本课学习目标

完成本课后，你将能够：
- ✅ 了解GORM的基本概念和优势
- ✅ 学会安装和初始化GORM
- ✅ 掌握连接各种数据库的方法
- ✅ 理解GORM的核心组件
- ✅ 创建第一个GORM应用

---

## 一、GORM简介

### 1.1 什么是GORM？

GORM（Go Object-Relational Mapping）是Go语言中最流行的ORM框架，它提供了：

- **全功能ORM**：支持关联、事务、迁移等
- **开发者友好**：简洁的API，强大的功能
- **数据库支持**：MySQL、PostgreSQL、SQLite、SQL Server等
- **自动迁移**：根据模型自动创建和更新表结构
- **钩子函数**：支持生命周期钩子
- **预加载**：智能的关联预加载

### 1.2 为什么使用GORM？

#### 传统方式 vs GORM

**传统database/sql方式**：

```go
// 需要手动编写SQL
rows, err := db.Query("SELECT id, name, email FROM users WHERE id = ?", userID)
if err != nil {
    return err
}
defer rows.Close()

var user User
for rows.Next() {
    err := rows.Scan(&user.ID, &user.Name, &user.Email)
    if err != nil {
        return err
    }
}
```

**使用GORM方式**：

```go
// 简洁的API，自动处理SQL
var user User
db.First(&user, userID)
```

#### GORM的优势

1. **代码简洁**：减少大量样板代码
2. **类型安全**：编译时检查，减少运行时错误
3. **自动迁移**：根据模型自动创建表结构
4. **关联支持**：轻松处理复杂的关联关系
5. **查询构建器**：链式调用，构建复杂查询

---

## 二、安装和初始化

### 2.1 安装GORM

```bash
# 安装GORM核心包
go get -u gorm.io/gorm

# 安装数据库驱动（根据使用的数据库选择）
go get -u gorm.io/driver/mysql        # MySQL
go get -u gorm.io/driver/postgres    # PostgreSQL
go get -u gorm.io/driver/sqlite       # SQLite
go get -u gorm.io/driver/sqlserver    # SQL Server
```

### 2.2 导入包

```go
import (
    "gorm.io/gorm"
    "gorm.io/driver/mysql"
)
```

---

## 三、数据库连接

### 3.1 连接MySQL

```go
package main

import (
    "fmt"
    "log"

    "gorm.io/driver/mysql"
    "gorm.io/gorm"
)

func main() {
    // MySQL连接字符串
    dsn := "user:password@tcp(127.0.0.1:3306)/dbname?charset=utf8mb4&parseTime=True&loc=Local"

    // 打开数据库连接
    db, err := gorm.Open(mysql.Open(dsn), &gorm.Config{})
    if err != nil {
        log.Fatal("连接数据库失败:", err)
    }

    fmt.Println("数据库连接成功！")
    
    // 使用db进行数据库操作
    _ = db
}
```

#### MySQL连接字符串格式

```
[username[:password]@][protocol[(address)]]/dbname[?param1=value1&...&paramN=valueN]
```

**常用参数**：
- `charset=utf8mb4`：字符集
- `parseTime=True`：解析时间
- `loc=Local`：时区设置
- `timeout=10s`：连接超时

### 3.2 连接PostgreSQL

```go
package main

import (
    "fmt"
    "log"

    "gorm.io/driver/postgres"
    "gorm.io/gorm"
)

func main() {
    // PostgreSQL连接字符串
    dsn := "host=localhost user=gorm password=gorm dbname=gorm port=9920 sslmode=disable TimeZone=Asia/Shanghai"

    db, err := gorm.Open(postgres.Open(dsn), &gorm.Config{})
    if err != nil {
        log.Fatal("连接数据库失败:", err)
    }

    fmt.Println("数据库连接成功！")
}
```

### 3.3 连接SQLite

```go
package main

import (
    "fmt"
    "log"

    "gorm.io/driver/sqlite"
    "gorm.io/gorm"
)

func main() {
    // SQLite连接（文件路径）
    db, err := gorm.Open(sqlite.Open("test.db"), &gorm.Config{})
    if err != nil {
        log.Fatal("连接数据库失败:", err)
    }

    fmt.Println("数据库连接成功！")
}
```

### 3.4 连接池配置

```go
package main

import (
    "fmt"
    "log"
    "time"

    "gorm.io/driver/mysql"
    "gorm.io/gorm"
)

func main() {
    dsn := "user:password@tcp(127.0.0.1:3306)/dbname?charset=utf8mb4&parseTime=True&loc=Local"

    db, err := gorm.Open(mysql.Open(dsn), &gorm.Config{})
    if err != nil {
        log.Fatal("连接数据库失败:", err)
    }

    // 获取底层的sql.DB对象进行连接池配置
    sqlDB, err := db.DB()
    if err != nil {
        log.Fatal("获取数据库连接失败:", err)
    }

    // 设置连接池参数
    sqlDB.SetMaxIdleConns(10)                  // 设置空闲连接池中连接的最大数量
    sqlDB.SetMaxOpenConns(100)                 // 设置打开数据库连接的最大数量
    sqlDB.SetConnMaxLifetime(time.Hour)        // 设置连接可复用的最大时间

    fmt.Println("数据库连接池配置完成！")
}
```

---

## 四、第一个GORM应用

### 4.1 定义模型

```go
package main

import (
    "gorm.io/gorm"
)

// User 用户模型
type User struct {
    ID    uint   `gorm:"primaryKey"`
    Name  string
    Email string `gorm:"uniqueIndex"`
    Age   int
}
```

### 4.2 自动迁移

```go
package main

import (
    "fmt"
    "log"

    "gorm.io/driver/sqlite"
    "gorm.io/gorm"
)

type User struct {
    ID    uint   `gorm:"primaryKey"`
    Name  string
    Email string `gorm:"uniqueIndex"`
    Age   int
}

func main() {
    // 连接数据库
    db, err := gorm.Open(sqlite.Open("test.db"), &gorm.Config{})
    if err != nil {
        log.Fatal("连接数据库失败:", err)
    }

    // 自动迁移：根据模型创建或更新表结构
    err = db.AutoMigrate(&User{})
    if err != nil {
        log.Fatal("迁移失败:", err)
    }

    fmt.Println("表创建成功！")
}
```

### 4.3 创建记录

```go
package main

import (
    "fmt"
    "log"

    "gorm.io/driver/sqlite"
    "gorm.io/gorm"
)

type User struct {
    ID    uint   `gorm:"primaryKey"`
    Name  string
    Email string `gorm:"uniqueIndex"`
    Age   int
}

func main() {
    db, err := gorm.Open(sqlite.Open("test.db"), &gorm.Config{})
    if err != nil {
        log.Fatal("连接数据库失败:", err)
    }

    // 创建用户
    user := User{
        Name:  "张三",
        Email: "zhangsan@example.com",
        Age:   25,
    }

    result := db.Create(&user)
    if result.Error != nil {
        log.Fatal("创建失败:", result.Error)
    }

    fmt.Printf("用户创建成功！ID: %d\n", user.ID)
}
```

### 4.4 查询记录

```go
package main

import (
    "fmt"
    "log"

    "gorm.io/driver/sqlite"
    "gorm.io/gorm"
)

type User struct {
    ID    uint   `gorm:"primaryKey"`
    Name  string
    Email string `gorm:"uniqueIndex"`
    Age   int
}

func main() {
    db, err := gorm.Open(sqlite.Open("test.db"), &gorm.Config{})
    if err != nil {
        log.Fatal("连接数据库失败:", err)
    }

    // 查询单个用户
    var user User
    db.First(&user, 1)  // 查询ID为1的用户

    fmt.Printf("用户: %+v\n", user)

    // 查询所有用户
    var users []User
    db.Find(&users)

    fmt.Printf("所有用户: %+v\n", users)
}
```

---

## 五、GORM核心组件

### 5.1 DB对象

`*gorm.DB` 是GORM的核心对象，所有数据库操作都通过它进行：

```go
type DB struct {
    *Config
    Error        error
    RowsAffected int64
    Statement    *Statement
    // ...
}
```

### 5.2 链式调用

GORM支持链式调用，让代码更加简洁：

```go
// 链式调用示例
db.Where("age > ?", 18).
   Where("name LIKE ?", "%张%").
   Order("created_at DESC").
   Limit(10).
   Find(&users)
```

### 5.3 错误处理

GORM的所有操作都会在`DB.Error`中记录错误：

```go
var user User
result := db.First(&user, 999)

if result.Error != nil {
    if errors.Is(result.Error, gorm.ErrRecordNotFound) {
        fmt.Println("记录不存在")
    } else {
        fmt.Println("查询失败:", result.Error)
    }
}
```

### 5.4 结果处理

```go
// 获取影响的行数
result := db.Create(&user)
fmt.Printf("影响行数: %d\n", result.RowsAffected)

// 检查错误
if result.Error != nil {
    log.Fatal(result.Error)
}
```

---

## 六、配置选项

### 6.1 GORM配置

```go
db, err := gorm.Open(mysql.Open(dsn), &gorm.Config{
    // 跳过默认事务
    SkipDefaultTransaction: false,
    
    // 命名策略
    NamingStrategy: schema.NamingStrategy{
        TablePrefix:   "t_",  // 表前缀
        SingularTable: true,  // 使用单数表名
    },
    
    // 日志级别
    Logger: logger.Default.LogMode(logger.Info),
    
    // 禁用外键约束
    DisableForeignKeyConstraintWhenMigrating: true,
    
    // 准备语句缓存
    PrepareStmt: true,
})
```

### 6.2 日志配置

```go
import (
    "gorm.io/gorm/logger"
)

db, err := gorm.Open(mysql.Open(dsn), &gorm.Config{
    Logger: logger.Default.LogMode(logger.Info),  // 显示所有SQL
    // Logger: logger.Default.LogMode(logger.Warn),  // 只显示警告和错误
    // Logger: logger.Default.LogMode(logger.Error), // 只显示错误
    // Logger: logger.Default.LogMode(logger.Silent), // 不显示日志
})
```

### 6.3 自定义日志

```go
import (
    "log"
    "os"
    "time"

    "gorm.io/gorm/logger"
)

newLogger := logger.New(
    log.New(os.Stdout, "\r\n", log.LstdFlags), // io writer
    logger.Config{
        SlowThreshold:             time.Second,   // 慢SQL阈值
        LogLevel:                  logger.Info,   // 日志级别
        IgnoreRecordNotFoundError: true,         // 忽略ErrRecordNotFound错误
        Colorful:                  false,        // 禁用彩色打印
    },
)

db, err := gorm.Open(mysql.Open(dsn), &gorm.Config{
    Logger: newLogger,
})
```

---

## 七、完整示例

### 7.1 用户管理系统示例

```go
package main

import (
    "fmt"
    "log"

    "gorm.io/driver/sqlite"
    "gorm.io/gorm"
)

// User 用户模型
type User struct {
    ID    uint   `gorm:"primaryKey"`
    Name  string `gorm:"size:100;not null"`
    Email string `gorm:"size:255;uniqueIndex;not null"`
    Age   int    `gorm:"default:0"`
}

func main() {
    // 连接数据库
    db, err := gorm.Open(sqlite.Open("users.db"), &gorm.Config{})
    if err != nil {
        log.Fatal("连接数据库失败:", err)
    }

    // 自动迁移
    err = db.AutoMigrate(&User{})
    if err != nil {
        log.Fatal("迁移失败:", err)
    }

    fmt.Println("=== 创建用户 ===")
    // 创建用户
    user1 := User{Name: "张三", Email: "zhangsan@example.com", Age: 25}
    db.Create(&user1)
    fmt.Printf("创建用户: %+v\n", user1)

    user2 := User{Name: "李四", Email: "lisi@example.com", Age: 30}
    db.Create(&user2)
    fmt.Printf("创建用户: %+v\n", user2)

    fmt.Println("\n=== 查询用户 ===")
    // 查询所有用户
    var users []User
    db.Find(&users)
    for _, user := range users {
        fmt.Printf("用户: %+v\n", user)
    }

    fmt.Println("\n=== 查询单个用户 ===")
    // 查询单个用户
    var user User
    db.First(&user, "email = ?", "zhangsan@example.com")
    fmt.Printf("找到用户: %+v\n", user)

    fmt.Println("\n=== 更新用户 ===")
    // 更新用户
    db.Model(&user).Update("Age", 26)
    fmt.Printf("更新后: %+v\n", user)

    fmt.Println("\n=== 删除用户 ===")
    // 删除用户
    db.Delete(&user2)
    fmt.Println("用户已删除")

    // 再次查询所有用户
    db.Find(&users)
    fmt.Printf("\n剩余用户数量: %d\n", len(users))
}
```

---

## 八、常见问题

### 8.1 连接失败

**问题**：无法连接到数据库

**解决方案**：
1. 检查数据库服务是否启动
2. 验证连接字符串是否正确
3. 检查网络连接和防火墙设置
4. 确认数据库用户权限

```go
// 添加连接超时和重试
dsn := "user:password@tcp(127.0.0.1:3306)/dbname?charset=utf8mb4&parseTime=True&loc=Local&timeout=10s"
```

### 8.2 表不存在

**问题**：查询时提示表不存在

**解决方案**：
1. 确保已运行`AutoMigrate`
2. 检查模型定义是否正确
3. 验证数据库连接是否指向正确的数据库

```go
// 确保迁移执行
if err := db.AutoMigrate(&User{}); err != nil {
    log.Fatal("迁移失败:", err)
}
```

### 8.3 字符编码问题

**问题**：中文乱码

**解决方案**：
1. 确保连接字符串包含正确的字符集
2. 数据库表使用utf8mb4字符集

```go
// MySQL连接字符串
dsn := "user:password@tcp(127.0.0.1:3306)/dbname?charset=utf8mb4&parseTime=True&loc=Local"
```

---

## 总结

### GORM入门要点

1. **安装和配置**：
   - 安装GORM核心包和数据库驱动
   - 配置数据库连接字符串
   - 设置连接池参数

2. **基本使用**：
   - 定义模型结构体
   - 使用AutoMigrate创建表
   - 使用Create、Find等基本操作

3. **核心概念**：
   - DB对象是操作入口
   - 支持链式调用
   - 错误处理通过Error字段

4. **最佳实践**：
   - 合理配置连接池
   - 设置适当的日志级别
   - 始终检查错误

### 下一步学习

- **第2课**：深入学习模型定义和GORM标签
- **第3课**：掌握完整的CRUD操作
- **第4课**：学习复杂的查询和条件

掌握GORM的基础使用后，你将能够快速开发数据库应用。在下一课中，我们将深入学习模型定义和GORM标签的使用。