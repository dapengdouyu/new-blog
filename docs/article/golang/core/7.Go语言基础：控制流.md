---
title: 7、Go语言基础：控制流
date: 2026-01-06
description: 深入理解Go语言的条件语句和循环语句
---

# 7、Go语言基础：控制流

本文档系统讲解Go语言的控制流语句，包括条件语句和循环语句，是编写有逻辑的程序的基础。

## 🎯 本课学习目标

完成本课后，你将能够：
- ✅ 掌握条件语句（if）的使用
- ✅ 掌握循环语句（for）的各种形式
- ✅ 理解循环控制语句（break、continue、标签）
- ✅ 掌握for-range循环的使用

---

## 一、条件语句（if）

### 1.1 基本if语句

```go
package main

import "fmt"

func main() {
    score := 85
    
    if score >= 90 {
        fmt.Println("优秀")
    } else if score >= 80 {
        fmt.Println("良好")
    } else {
        fmt.Println("需要努力")
    }
}
```

### 1.2 if的初始化语句

Go语言的if语句支持在条件前执行初始化语句：

```go
// 语法：if init; condition { }
if err := doSomething(); err != nil {
    fmt.Println("错误:", err)
}

// 常见用法：错误处理
if file, err := os.Open("file.txt"); err != nil {
    fmt.Println("打开文件失败:", err)
} else {
    defer file.Close()
    // 使用file
}
```

### 1.3 if的作用域

if语句中的初始化变量作用域仅限于if-else块：

```go
if x := 10; x > 5 {
    fmt.Println(x)  // 10
}
// fmt.Println(x)  // 编译错误：x未定义

// 如果需要在外层使用，在外层声明
var x int
if x = 10; x > 5 {
    fmt.Println(x)
}
fmt.Println(x)  // 10
```

### 1.4 常见模式

#### 1. 错误处理

```go
if err := function(); err != nil {
    return err
}
```

#### 2. 类型断言

```go
if value, ok := interfaceVar.(string); ok {
    fmt.Println("是字符串:", value)
}
```

#### 3. Map检查

```go
if value, exists := m["key"]; exists {
    fmt.Println("值:", value)
}
```

---

## 二、循环语句（for）

### 2.1 传统for循环

Go语言只有`for`一种循环语句，但支持多种形式：

```go
// 1. 传统三部分形式
for i := 0; i < 10; i++ {
    fmt.Println(i)
}

// 2. 类似while循环
i := 0
for i < 10 {
    fmt.Println(i)
    i++
}

// 3. 无限循环
for {
    // 需要break退出
    if condition {
        break
    }
}
```

### 2.2 for-range循环

用于遍历数组、切片、字符串、map和通道：

```go
// 遍历切片
nums := []int{1, 2, 3, 4, 5}
for index, value := range nums {
    fmt.Println(index, value)
}

// 只获取值
for _, value := range nums {
    fmt.Println(value)
}

// 只获取索引
for index := range nums {
    fmt.Println(index)
}

// 遍历字符串（按rune）
str := "Hello"
for i, char := range str {
    fmt.Printf("%d: %c\n", i, char)
}

// 遍历map
m := map[string]int{"a": 1, "b": 2}
for key, value := range m {
    fmt.Println(key, value)
}
```

### 2.3 循环控制

#### break和continue

```go
// break：跳出循环
for i := 0; i < 10; i++ {
    if i == 5 {
        break  // 跳出循环
    }
    fmt.Println(i)  // 0, 1, 2, 3, 4
}

// continue：跳过本次循环
for i := 0; i < 10; i++ {
    if i%2 == 0 {
        continue  // 跳过偶数
    }
    fmt.Println(i)  // 1, 3, 5, 7, 9
}
```

#### 标签（Label）

使用标签可以跳出外层循环：

```go
outer:
for i := 0; i < 3; i++ {
    for j := 0; j < 3; j++ {
        if i == 1 && j == 1 {
            break outer  // 跳出外层循环
        }
        fmt.Println(i, j)
    }
}
```

### 2.4 for循环的常见模式

#### 1. 遍历并修改切片

```go
nums := []int{1, 2, 3, 4, 5}
for i := range nums {
    nums[i] *= 2  // 修改原切片
}
fmt.Println(nums)  // [2 4 6 8 10]
```

#### 2. 查找元素

```go
nums := []int{1, 2, 3, 4, 5}
target := 3
found := false

for _, num := range nums {
    if num == target {
        found = true
        break
    }
}
```

#### 3. 累加/聚合

```go
nums := []int{1, 2, 3, 4, 5}
sum := 0
for _, num := range nums {
    sum += num
}
fmt.Println(sum)  // 15
```

---

## 三、综合示例

```go
package main

import (
    "fmt"
    "math/rand"
)

func main() {
    // 生成随机数切片
    numbers := make([]int, 10)
    for i := range numbers {
        numbers[i] = rand.Intn(100)
    }
    
    fmt.Println("原始数据:", numbers)
    
    // 查找最大值和最小值
    max, min := numbers[0], numbers[0]
    for _, num := range numbers {
        if num > max {
            max = num
        }
        if num < min {
            min = num
        }
    }
    fmt.Printf("最大值: %d, 最小值: %d\n", max, min)
    
    // 计算平均值
    sum := 0
    for _, num := range numbers {
        sum += num
    }
    avg := float64(sum) / float64(len(numbers))
    fmt.Printf("平均值: %.2f\n", avg)
    
    // 筛选偶数
    evens := []int{}
    for _, num := range numbers {
        if num%2 == 0 {
            evens = append(evens, num)
        }
    }
    fmt.Println("偶数:", evens)
}
```

---

## 总结

1. **if语句**：支持初始化语句，作用域限制在if-else块内，常用于错误处理、类型断言和map检查
2. **for循环**：唯一的循环语句，支持多种形式（传统for、while风格、无限循环）
3. **for-range循环**：用于遍历数组、切片、字符串、map和通道，可以获取索引和值
4. **循环控制**：使用break跳出循环，continue跳过本次循环，标签可以跳出外层循环

掌握这些控制流语句，是编写有逻辑的Go程序的基础。在实际开发中，要特别注意：
- if语句中初始化变量的作用域
- for-range循环中变量的作用域（Go 1.22已修复）
- 使用标签跳出嵌套循环的技巧
