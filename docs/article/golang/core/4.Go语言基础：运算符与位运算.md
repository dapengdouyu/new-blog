---
title: 4、Go语言基础：运算符与位运算
date: 2026-01-09
description: 深入学习Go语言的运算符，包括算术、比较、逻辑、赋值运算符以及位运算的详细用法
---

# 4、Go语言基础：运算符与位运算

本文档深入讲解Go语言的所有运算符，包括算术、比较、逻辑、赋值运算符以及位运算的详细用法。

## 🎯 本课学习目标

完成本课后，你将能够：
- ✅ 掌握算术运算符的使用
- ✅ 理解比较运算符和逻辑运算符
- ✅ 掌握赋值运算符的用法
- ✅ 深入理解位运算的原理和应用
- ✅ 掌握运算符优先级
- ✅ 学会在实际项目中使用位运算

---

## 前置知识

- **第1课：基础语法与变量常量** - 了解变量和常量的基本概念
- **第2课：数据类型与类型转换** - 了解基本数据类型

---

## 一、算术运算符

### 1.1 基本算术运算符

| 运算符 | 说明 | 示例 |
|--------|------|------|
| `+` | 加法 | `3 + 2 = 5` |
| `-` | 减法 | `5 - 2 = 3` |
| `*` | 乘法 | `3 * 2 = 6` |
| `/` | 除法 | `6 / 2 = 3` |
| `%` | 取余（模运算） | `7 % 3 = 1` |

```go
package main

import "fmt"

func main() {
    a, b := 10, 3
    
    fmt.Printf("a + b = %d\n", a+b)   // 13
    fmt.Printf("a - b = %d\n", a-b)   // 7
    fmt.Printf("a * b = %d\n", a*b)   // 30
    fmt.Printf("a / b = %d\n", a/b)   // 3（整数除法）
    fmt.Printf("a %% b = %d\n", a%b)  // 1（取余）
    
    // 浮点数运算
    x, y := 10.0, 3.0
    fmt.Printf("x / y = %.2f\n", x/y)  // 3.33
}
```

### 1.2 整数除法的特点

```go
package main

import "fmt"

func main() {
    // 整数除法：结果也是整数（向下取整）
    fmt.Println(10 / 3)    // 3
    fmt.Println(10 / 3.0)  // 3.333...（浮点数除法）
    
    // 负数除法
    fmt.Println(-10 / 3)   // -3
    fmt.Println(10 / -3)   // -3
    
    // 取余运算
    fmt.Println(10 % 3)    // 1
    fmt.Println(-10 % 3)   // -1
    fmt.Println(10 % -3)   // 1
}
```

### 1.3 自增和自减

Go语言只有后置自增和自减，没有前置：

```go
package main

import "fmt"

func main() {
    x := 5
    
    x++  // x = x + 1
    fmt.Println(x)  // 6
    
    x--  // x = x - 1
    fmt.Println(x)  // 5
    
    // 注意：++和--是语句，不是表达式
    // y := x++  // 编译错误：syntax error: unexpected ++
    
    // 正确用法
    x++
    y := x
    fmt.Println(y)  // 6
}
```

---

## 二、比较运算符

### 2.1 基本比较运算符

| 运算符 | 说明 | 示例 |
|--------|------|------|
| `==` | 等于 | `5 == 5` → `true` |
| `!=` | 不等于 | `5 != 3` → `true` |
| `<` | 小于 | `3 < 5` → `true` |
| `<=` | 小于等于 | `3 <= 5` → `true` |
| `>` | 大于 | `5 > 3` → `true` |
| `>=` | 大于等于 | `5 >= 3` → `true` |

```go
package main

import "fmt"

func main() {
    a, b := 5, 3
    
    fmt.Printf("a == b: %t\n", a == b)  // false
    fmt.Printf("a != b: %t\n", a != b)  // true
    fmt.Printf("a < b: %t\n", a < b)    // false
    fmt.Printf("a <= b: %t\n", a <= b)  // false
    fmt.Printf("a > b: %t\n", a > b)    // true
    fmt.Printf("a >= b: %t\n", a >= b)  // true
}
```

### 2.2 字符串比较

```go
package main

import "fmt"

func main() {
    s1, s2 := "apple", "banana"
    
    fmt.Printf("s1 == s2: %t\n", s1 == s2)  // false
    fmt.Printf("s1 < s2: %t\n", s1 < s2)   // true（按字典序）
    fmt.Printf("s1 > s2: %t\n", s1 > s2)   // false
}
```

---

## 三、逻辑运算符

### 3.1 基本逻辑运算符

| 运算符 | 说明 | 示例 |
|--------|------|------|
| `&&` | 逻辑与（短路求值） | `true && false` → `false` |
| `||` | 逻辑或（短路求值） | `true || false` → `true` |
| `!` | 逻辑非 | `!true` → `false` |

```go
package main

import "fmt"

func main() {
    a, b := true, false
    
    fmt.Printf("a && b: %t\n", a && b)  // false
    fmt.Printf("a || b: %t\n", a || b)  // true
    fmt.Printf("!a: %t\n", !a)          // false
    fmt.Printf("!b: %t\n", !b)          // true
}
```

### 3.2 短路求值

```go
package main

import "fmt"

func condition1() bool {
    fmt.Println("condition1 被调用")
    return true
}

func condition2() bool {
    fmt.Println("condition2 被调用")
    return false
}

func main() {
    // && 短路：第一个为false，不执行第二个
    if condition2() && condition1() {
        fmt.Println("不会执行")
    }
    // 输出：condition2 被调用
    
    // || 短路：第一个为true，不执行第二个
    if condition1() || condition2() {
        fmt.Println("执行")
    }
    // 输出：condition1 被调用
    //       执行
}
```

---

## 四、赋值运算符

### 4.1 基本赋值运算符

| 运算符 | 说明 | 等价于 |
|--------|------|--------|
| `=` | 赋值 | `x = 5` |
| `+=` | 加后赋值 | `x += 3` → `x = x + 3` |
| `-=` | 减后赋值 | `x -= 3` → `x = x - 3` |
| `*=` | 乘后赋值 | `x *= 3` → `x = x * 3` |
| `/=` | 除后赋值 | `x /= 3` → `x = x / 3` |
| `%=` | 取余后赋值 | `x %= 3` → `x = x % 3` |

```go
package main

import "fmt"

func main() {
    x := 10
    
    x += 5   // x = 15
    fmt.Println(x)
    
    x -= 3   // x = 12
    fmt.Println(x)
    
    x *= 2   // x = 24
    fmt.Println(x)
    
    x /= 4   // x = 6
    fmt.Println(x)
    
    x %= 4   // x = 2
    fmt.Println(x)
}
```

### 4.2 位运算赋值运算符

```go
package main

import "fmt"

func main() {
    x := 10  // 1010 (二进制)
    
    x &= 5   // x = x & 5 → 0 (1010 & 0101 = 0000)
    fmt.Println(x)
    
    x = 10
    x |= 5   // x = x | 5 → 15 (1010 | 0101 = 1111)
    fmt.Println(x)
    
    x = 10
    x ^= 5   // x = x ^ 5 → 15 (1010 ^ 0101 = 1111)
    fmt.Println(x)
    
    x = 10
    x <<= 2  // x = x << 2 → 40 (左移2位)
    fmt.Println(x)
    
    x = 10
    x >>= 2  // x = x >> 2 → 2 (右移2位)
    fmt.Println(x)
}
```

---

## 五、位运算

### 5.1 位运算基础

位运算直接操作整数的二进制表示：

| 运算符 | 说明 | 示例 |
|--------|------|------|
| `&` | 按位与 | `5 & 3 = 1` (101 & 011 = 001) |
| `|` | 按位或 | `5 | 3 = 7` (101 | 011 = 111) |
| `^` | 按位异或 | `5 ^ 3 = 6` (101 ^ 011 = 110) |
| `&^` | 按位清除（AND NOT） | `5 &^ 3 = 4` (101 &^ 011 = 100) |
| `<<` | 左移 | `5 << 2 = 20` (101 << 2 = 10100) |
| `>>` | 右移 | `5 >> 2 = 1` (101 >> 2 = 001) |

```go
package main

import "fmt"

func main() {
    a, b := 5, 3  // 5 = 101, 3 = 011 (二进制)
    
    fmt.Printf("a = %d (%b), b = %d (%b)\n", a, a, b, b)
    
    // 按位与：两个位都为1时结果为1
    fmt.Printf("a & b = %d (%b)\n", a&b, a&b)  // 1 (001)
    
    // 按位或：至少一个位为1时结果为1
    fmt.Printf("a | b = %d (%b)\n", a|b, a|b)  // 7 (111)
    
    // 按位异或：两个位不同时结果为1
    fmt.Printf("a ^ b = %d (%b)\n", a^b, a^b)  // 6 (110)
    
    // 按位清除：清除a中与b相同的位
    fmt.Printf("a &^ b = %d (%b)\n", a&^b, a&^b)  // 4 (100)
}
```

### 5.2 按位与（&）

```go
package main

import "fmt"

func main() {
    // 检查特定位是否为1
    flags := 0b1010  // 10 (二进制：1010)
    
    // 检查第0位（最低位）
    if flags&1 != 0 {
        fmt.Println("第0位是1")
    } else {
        fmt.Println("第0位是0")
    }
    
    // 检查第1位
    if flags&2 != 0 {
        fmt.Println("第1位是1")
    }
    
    // 检查第3位
    if flags&8 != 0 {
        fmt.Println("第3位是1")
    }
    
    // 清除特定位
    flags &= ^1  // 清除第0位
    fmt.Printf("清除第0位后: %b\n", flags)  // 1000
}
```

### 5.3 按位或（|）

```go
package main

import "fmt"

func main() {
    // 设置特定位为1
    flags := 0b0000
    
    flags |= 1   // 设置第0位
    fmt.Printf("设置第0位: %b\n", flags)  // 0001
    
    flags |= 4   // 设置第2位
    fmt.Printf("设置第2位: %b\n", flags)  // 0101
    
    flags |= 8   // 设置第3位
    fmt.Printf("设置第3位: %b\n", flags)  // 1101
}
```

### 5.4 按位异或（^）

```go
package main

import "fmt"

func main() {
    // 切换特定位（0变1，1变0）
    flags := 0b1010
    
    flags ^= 1   // 切换第0位
    fmt.Printf("切换第0位: %b\n", flags)  // 1011
    
    flags ^= 1   // 再次切换第0位
    fmt.Printf("再次切换: %b\n", flags)   // 1010
    
    // 交换两个变量（不使用临时变量）
    a, b := 5, 3
    fmt.Printf("交换前: a=%d, b=%d\n", a, b)
    
    a ^= b
    b ^= a
    a ^= b
    fmt.Printf("交换后: a=%d, b=%d\n", a, b)  // a=3, b=5
}
```

### 5.5 按位清除（&^）

```go
package main

import "fmt"

func main() {
    // 清除特定位
    flags := 0b1111  // 15 (二进制：1111)
    
    flags &^= 1   // 清除第0位
    fmt.Printf("清除第0位: %b\n", flags)  // 1110
    
    flags &^= 4   // 清除第2位
    fmt.Printf("清除第2位: %b\n", flags)  // 1010
    
    // 等价于：flags = flags & (^mask)
    flags = 0b1111
    mask := 0b0101  // 要清除的位
    flags = flags &^ mask
    fmt.Printf("清除多个位: %b\n", flags)  // 1010
}
```

### 5.6 位移运算

#### 左移（<<）

```go
package main

import "fmt"

func main() {
    x := 5  // 101 (二进制)
    
    // 左移：相当于乘以2的n次方
    fmt.Printf("5 << 1 = %d (%b)\n", x<<1, x<<1)  // 10 (1010) = 5 * 2
    fmt.Printf("5 << 2 = %d (%b)\n", x<<2, x<<2)  // 20 (10100) = 5 * 4
    fmt.Printf("5 << 3 = %d (%b)\n", x<<3, x<<3)  // 40 (101000) = 5 * 8
    
    // 快速计算2的幂
    power2 := 1 << 10  // 2^10 = 1024
    fmt.Printf("2^10 = %d\n", power2)
}
```

#### 右移（>>）

```go
package main

import "fmt"

func main() {
    x := 20  // 10100 (二进制)
    
    // 右移：相当于除以2的n次方（向下取整）
    fmt.Printf("20 >> 1 = %d (%b)\n", x>>1, x>>1)  // 10 (1010) = 20 / 2
    fmt.Printf("20 >> 2 = %d (%b)\n", x>>2, x>>2)  // 5 (101) = 20 / 4
    fmt.Printf("20 >> 3 = %d (%b)\n", x>>3, x>>3)  // 2 (10) = 20 / 8
    
    // 负数右移（算术右移，保留符号位）
    y := -20
    fmt.Printf("-20 >> 2 = %d\n", y>>2)  // -5
}
```

### 5.7 位运算的实际应用

#### 1. 权限系统

```go
package main

import "fmt"

// 权限标志
const (
    Read   = 1 << iota  // 1 (001)
    Write                // 2 (010)
    Execute              // 4 (100)
)

type User struct {
    Name     string
    Permissions int
}

// 添加权限
func (u *User) AddPermission(perm int) {
    u.Permissions |= perm
}

// 移除权限
func (u *User) RemovePermission(perm int) {
    u.Permissions &^= perm
}

// 检查权限
func (u *User) HasPermission(perm int) bool {
    return u.Permissions&perm != 0
}

func main() {
    user := &User{Name: "Alice", Permissions: 0}
    
    // 添加权限
    user.AddPermission(Read)
    user.AddPermission(Write)
    
    fmt.Printf("权限: %b\n", user.Permissions)  // 011
    
    // 检查权限
    fmt.Printf("有读权限: %t\n", user.HasPermission(Read))    // true
    fmt.Printf("有执行权限: %t\n", user.HasPermission(Execute)) // false
    
    // 移除权限
    user.RemovePermission(Write)
    fmt.Printf("移除写权限后: %b\n", user.Permissions)  // 001
}
```

#### 2. 标志位操作

```go
package main

import "fmt"

// 状态标志
const (
    StatusNone    = 0
    StatusActive  = 1 << iota  // 1
    StatusPending               // 2
    StatusCompleted             // 4
    StatusError                 // 8
)

func main() {
    status := StatusNone
    
    // 设置多个状态
    status |= StatusActive
    status |= StatusPending
    fmt.Printf("状态: %b\n", status)  // 011
    
    // 检查状态
    if status&StatusActive != 0 {
        fmt.Println("状态是活跃的")
    }
    
    // 清除状态
    status &^= StatusPending
    fmt.Printf("清除Pending后: %b\n", status)  // 001
}
```

#### 3. 快速计算

```go
package main

import "fmt"

func main() {
    // 判断是否为2的幂
    isPowerOf2 := func(n int) bool {
        return n > 0 && (n&(n-1)) == 0
    }
    
    fmt.Println(isPowerOf2(8))   // true
    fmt.Println(isPowerOf2(9))   // false
    
    // 计算2的幂
    power := func(n int) int {
        return 1 << n
    }
    
    fmt.Printf("2^3 = %d\n", power(3))  // 8
    fmt.Printf("2^10 = %d\n", power(10)) // 1024
    
    // 快速除以2
    divideBy2 := func(n int) int {
        return n >> 1
    }
    
    fmt.Printf("20 / 2 = %d\n", divideBy2(20))  // 10
    
    // 快速乘以2
    multiplyBy2 := func(n int) int {
        return n << 1
    }
    
    fmt.Printf("20 * 2 = %d\n", multiplyBy2(20))  // 40
}
```

#### 4. 位掩码

```go
package main

import "fmt"

func main() {
    // 提取特定位
    extractBits := func(value, mask int) int {
        return value & mask
    }
    
    value := 0b11010110  // 214
    mask := 0b00001111   // 提取低4位
    
    result := extractBits(value, mask)
    fmt.Printf("提取低4位: %b\n", result)  // 0110
    
    // 设置特定位
    setBits := func(value, mask int) int {
        return value | mask
    }
    
    value = 0b00000000
    mask = 0b00001111
    
    result = setBits(value, mask)
    fmt.Printf("设置低4位: %b\n", result)  // 1111
}
```

---

## 六、运算符优先级

### 6.1 优先级表

从高到低：

| 优先级 | 运算符 | 说明 |
|--------|--------|------|
| 1 | `()` `[]` | 括号、索引 |
| 2 | `*` `/` `%` `<<` `>>` `&` `&^` | 乘除、位移、位与 |
| 3 | `+` `-` `\|` `^` | 加减、位或、位异或 |
| 4 | `==` `!=` `<` `<=` `>` `>=` | 比较运算符 |
| 5 | `&&` | 逻辑与 |
| 6 | `\|\|` | 逻辑或 |

```go
package main

import "fmt"

func main() {
    // 括号优先级最高
    result1 := (1 + 2) * 3  // 9
    result2 := 1 + 2*3      // 7
    
    fmt.Printf("(1+2)*3 = %d\n", result1)
    fmt.Printf("1+2*3 = %d\n", result2)
    
    // 算术 > 比较 > 逻辑
    x, y, z := 5, 3, 2
    result3 := x+y > z && x-y < z*2  // (5+3) > 2 && (5-3) < (2*2)
    fmt.Printf("结果: %t\n", result3)  // true
}
```

### 6.2 使用括号提高可读性

```go
package main

import "fmt"

func main() {
    // ❌ 不清晰
    result1 := a+b*c-d/e%f
    
    // ✅ 使用括号提高可读性
    result2 := (a + b*c) - (d/e%f)
    
    // 即使优先级正确，括号也能提高可读性
    result3 := (a > b) && (c < d) || (e == f)
}
```

---

## 七、综合示例

```go
package main

import "fmt"

// 使用位运算实现简单的权限系统
type Permission int

const (
    None Permission = 0
    Read  Permission = 1 << iota  // 1
    Write                         // 2
    Execute                       // 4
    Delete                        // 8
)

type File struct {
    Name        string
    Permissions Permission
}

func (f *File) AddPermission(p Permission) {
    f.Permissions |= p
}

func (f *File) RemovePermission(p Permission) {
    f.Permissions &^= p
}

func (f *File) HasPermission(p Permission) bool {
    return f.Permissions&p != 0
}

func (f *File) String() string {
    var perms []string
    if f.HasPermission(Read) {
        perms = append(perms, "Read")
    }
    if f.HasPermission(Write) {
        perms = append(perms, "Write")
    }
    if f.HasPermission(Execute) {
        perms = append(perms, "Execute")
    }
    if f.HasPermission(Delete) {
        perms = append(perms, "Delete")
    }
    return fmt.Sprintf("File: %s, Permissions: %v", f.Name, perms)
}

func main() {
    file := &File{Name: "document.txt", Permissions: None}
    
    // 添加权限
    file.AddPermission(Read)
    file.AddPermission(Write)
    file.AddPermission(Execute)
    
    fmt.Println(file)
    // 输出：File: document.txt, Permissions: [Read Write Execute]
    
    // 检查权限
    fmt.Printf("可读: %t, 可写: %t, 可执行: %t, 可删除: %t\n",
        file.HasPermission(Read),
        file.HasPermission(Write),
        file.HasPermission(Execute),
        file.HasPermission(Delete))
    
    // 移除权限
    file.RemovePermission(Execute)
    fmt.Println(file)
    // 输出：File: document.txt, Permissions: [Read Write]
}
```

---

## 总结

### 运算符要点

1. **算术运算符**：
   - `+` `-` `*` `/` `%`：基本运算
   - `++` `--`：自增自减（后置）

2. **比较运算符**：
   - `==` `!=` `<` `<=` `>` `>=`：返回布尔值

3. **逻辑运算符**：
   - `&&` `||` `!`：逻辑运算，支持短路求值

4. **赋值运算符**：
   - `=` `+=` `-=` `*=` `/=` `%=`：复合赋值

### 位运算要点

1. **基本位运算**：
   - `&`：按位与，用于检查/清除位
   - `|`：按位或，用于设置位
   - `^`：按位异或，用于切换位
   - `&^`：按位清除，用于清除特定位

2. **位移运算**：
   - `<<`：左移，相当于乘以2的n次方
   - `>>`：右移，相当于除以2的n次方

3. **实际应用**：
   - 权限系统
   - 标志位操作
   - 快速计算
   - 位掩码操作

### 最佳实践

1. **使用括号**：提高代码可读性
2. **位运算优化**：在需要高性能的场景使用位运算
3. **权限系统**：使用位运算实现灵活的权限控制
4. **注意溢出**：进行位移运算时注意数据范围

掌握运算符和位运算是编写高效Go程序的基础。在下一课中，我们将学习指针的使用。